# Producción detrás de Apache como reverse proxy.
# El frontend usa rutas relativas: el navegador hace fetch a /api/v1/...
# y Apache lo redirige a 127.0.0.1:3012. No hace falta URL absoluta.
VITE_API_URL=
